# TIMECORE 自主迭代路线图（2026-09-19 夜间 → 09-19 早 8:30）

> 授权：用户委托 AI 全自主推进（功能 / UI / 动效 / 音效），要求「眼前一亮、有创意」，
> 前提：**不让项目操作变得更复杂**（不加新按钮/新流程，优先零 UI 成本的环境智能）。
> 每定一个方向就在此记录；每完成一个大版本（=一个方向收口）即发版 GitHub。
> 红线不变：绝对时间节点 / 显示不回跳 / 到点必触发 / 辉光配额 / 全量 E2E 保持全绿。
> 本夜起点：v1.6.0 已发布（架构模块化基线，36 契约 674 断言 + 八组 cycle + 真机 e2e 全绿）。

## 选题原则

1. 零操作成本：所有新东西默认自动生效或默认关闭，绝不新增必用按钮。
2. 主题契合：一切围绕「时间装置」——让用户**感觉到时间的流动**，而不是多一个功能。
3. 每个方向独立可发版：做完一个发一个，中断随时安全。
4. 验证纪律：node --check → 相关 E2E → 全量七组套件（cycle19|21|24|26|27|30|31）→ 截图留档。

## 方向总览（状态：⬜未开始 / 🔨进行中 / ✅已发版）

| # | 方向 | 版本 | 一句话 | 状态 |
|---|---|---|---|---|
| 0 | 钟面动效四件套 | v1.3.0 | 数字tick/窗口缓动/形态morph/呼吸边 + win:state 即时推送 | ✅已发版 |
| A | 行星真实太阳 | v1.3.1 | 3D 星球昼夜终端线对准真实太阳位置——桌面上的地球现在几点就被哪边照亮 | ✅随 v1.3.1 发版 |
| B | 到点音景 | v1.3.1 | 零点释放音三层化（sub冲击+五声琶音声像展开+钟簇余韵）+ master 压缩器 | ✅随 v1.3.1 发版 |
| C | 钟面秒环与零点脉动 | v1.3.1 | 钟面边框即秒针（彗尾每秒绕边框一周）；倒计时归零钟面呼吸脉动 | ✅随 v1.3.1 发版 |
| D | 开场仪式 | v1.3.1 | ready-to-show 再显示防空窗、整体淡入、欢迎卡入场、核心能量唤醒 | ✅随 v1.3.1 发版 |
| E | 收口打磨 | v1.3.2 | README/截图更新、钟面缩放记忆、CI 自愈加固 | ✅已发版 |
| F | 星空生动化 | v1.3.3 | 远景闪烁星野 + 随机流星划过（每 30-90s，仅视觉零交互） | ✅已发版 |
| G | 卫星尾迹 | v1.4.0 | 两颗轨道卫星拖出 1s 渐隐光尾（解析回推，零开销），悬停钟面秒环增亮 | ✅已发版 |
| H | 到点流星雨 + 回归固化 | v1.4.1 | 归零瞬间星野增亮+三颗流星错峰齐落，与音景合成完整仪式；新增 cycle31 守护今晚特性 | ✅已发版 |
| I | 渲染降耗 | v1.5.0 | 钟面形态整帧跳过 GPU 渲染（42 draw calls/1.7 万三角形 → 0），退出即恢复 | ✅已发版 |
| 基线 | 架构模块化 | v1.6.0 | TypeScript 模块化重构收口（36 契约 674 断言 + 八组 cycle + 真机 e2e） | ✅已发版 |
| J | 真实月相 | v1.7.0 | 行星外圈小月亮按 29.53 天朔望周期绕行，明暗面由真实太阳方向照出——夜空三件套（日/月/星）集齐 | ✅已发版 |
| K | 一日进度环 | v1.7.1 | 3D 外圈极淡细环 24h 缓慢填满，一天流了多少一眼可见 | ✅已发版 |
| L | 音景昼夜四态 | v1.8.0 | 到点琶音按节点时刻的晨/昼/暮/夜自动换五声音阶，零设置项 | ✅已发版 |
| M | 每日击拍统计 | v1.9.0 | 设置抽屉一行被动展示：今日击拍/最高连击/准确率 + 近 7 日 tooltip，本地存 30 天 | ✅已发版 |
| N | 整点深呼吸 | v1.10.0 | 每个真实整点星球一次缓慢深脉冲（纯视觉无声音），布防期间自动让位 | ✅已发版 |
| O | 月相夜光 + 文档收口 | v1.11.0 | 夜光随真实月相盈亏调制；重拍 README 主图、功能清单对齐 | ✅已发版 |
| P | 音效主题包 | v1.12.0 | 硅晶/钟琴/木质/芯片四套合成音色，设置抽屉下拉切换，tc.sndpack 持久化 | ✅已发版 |
| Q | 年度进度环 | v1.13.0 | 日环之外再一圈极淡年环按年填充（闰年感知），时→日→年三尺度闭环 | ✅已发版 |
| R | 到点历史统计 | v1.14.0 | 抽屉被动一行「今日到点 N · 累计 M」，累计每满 100 补一颗流星 | ✅已发版 |
| S | 太阳赤纬 | v1.15.0 | 太阳高度随季节 ±23.44° 真值摆动（视觉振幅 ±0.5），昼夜分割随季节倾斜 | ✅已发版 |
| T | 闲置自流动 | v1.16.0 | 无输入 10 分钟界面退场+相机极缓自转，布防等触发期间不生效 | ✅已发版 |
| U | 击拍黄金时段 | v1.17.0 | 击拍统计扩 24 小时分布，tooltip 标出近 30 天最准时段 | ✅已发版 |
| V | 远期目标日定时 | v1.18.0 | 定时栏新增可选日期，「周五发布会倒计时」类远期目标一键布防 | ✅已发版 |
| W | 闲置降帧 | v1.19.0 | 闲置态渲染降到 ~10fps（时间基准修正，漂移不减速），与闲置自流动三件套闭环 | ✅已发版 |
| X | 选点截图存档 | v1.20.0 | 用户反馈：选点截图按动作落盘 userData，重启后打开浮层仍能看到当时的图 | ✅已发版 |

> 合并说明：A-D 四个方向均零操作成本、彼此独立且全部 E2E 绿，合并为一个 v1.3.1 发布，避免连发多版刷屏。

### 方向 F：星空生动化（v1.3.3）
- 动机：星球背后是空的——一片会呼吸的远景星野 + 偶发流星，让「悬浮在太空」成立。
- 预期：约 320 颗远星（自定义 shader，相位错开的慢闪烁）；流星每 30-90s 一颗，拉伸光斑 0.9s 划过，质量低档不显示；`TC.Scene.meteor()` 强制触发（测试）。

### 发版事故记录（方向 0/E 之间）
- v1.3.0 首跑 CI 在 Build Windows installer 挂死 35 分钟（v1.2.4 同步骡全程 97s）→ 取消 rerun 后 2 分钟成功。
- v1.3.1 连续三次挂死（含镜像 env 后仍挂）。日志揭示真相：**不是死锁，是 GitHub runner 到发布 CDN 的上行极慢**（publish 阶段 3 分钟才 overwrite 一个文件），长等待后其实会完成。
- 治理：release.yml 加 `timeout-minutes: 15` + pwsh 重试 3 次 + npmmirror 双镜像（electron 与 electron-builder-binaries）。此后 tag→CI→PATCH 流程可自愈，无需人工盯守。

## 逐方向记录

### 收官备注（2026-09-25 凌晨 03:25）
- 本夜共发 8 版：v1.12.0 音效主题包 / v1.13.0 年度进度环 / v1.14.0 到点历史统计 / v1.15.0 太阳赤纬 / v1.16.0 闲置自流动 / v1.17.0 击拍黄金时段 / v1.18.0 远期目标日定时 / v1.19.0 闲置降帧。选题来源：subagent 脑暴菜单 5 方向（全收口）+ 自主追加 3 方向。
- 门禁基线演进：45 契约文件 1198 断言（+25 项）、cycle33 扩至 21 断言、九组 cycle + 无线真机 e2e 每版全绿。
- 封面图重拍并通过逐像素验收（APPROVE：满月 Ø84-88px 居中、星球 r≈95-100、0 黑块 0 残影；双环极淡标 Unverified 属设计本意）。
- 教训入库：深夜屏幕关闭致 CDP captureScreenshot 挂死（先唤醒显示器）；未授权新 USB 设备 screencap 挂死（adb reconnect 摘除）；作用域提升需核对 TC 出口引用。

### 方向 X：选点截图存档（v1.20.0，2026-09-25 凌晨，用户反馈驱动）
- 动机：用户反馈「每次打开都看不到之前截图的位置」——选点截图 `_shot` 设计上只驻留内存（防 base64 撑爆 localStorage 配额），应用重启后红点坐标还在、当时的图没了，上下文断裂。
- 实现：新增可选 `shotStore` 端口——截屏成功即按动作 id 存 `userData/adb-shots/<id>.png`（主进程新 IPC `adb:shot-save/load`，文件名白名单 `[a-z0-9_-]{1,64}` 防路径注入）；打开浮层时内存无图则自动从存档恢复，未命中回落重新截屏；浏览器模式不注入端口，行为与旧版一致；配置存储仍不存 base64（配额红线不动）。
- 验证：契约 +8 项（存档/重启恢复零 exec/未命中回落）；cycle33 扩至 22 断言（真实 IPC 磁盘往返 + `../evil` 注入拒绝）；45 契约 1206 断言 + 九组 cycle + 无线真机 e2e 全绿。
### 方向 W：闲置降帧（v1.19.0，2026-09-25 凌晨）
- 动机：v1.16 闲置自流动让界面淡出了，但 WebGL 仍对看不见的场景全速渲染——v1.5.0 揭露过的「隐形电暖器」在闲置态复活。淡出+慢转+降帧三件套才是完整闭环。
- 实现：render 循环 idle 态累积 dt，不足 100ms 跳帧（~10fps）；渲染帧以 idleAcc 真实累积时长为时间基准——相机漂移/粒子等时间驱动更新不因降帧减速（首版漏了这点，cycle33 的漂移量断言抓出后修正）。
- 验证：cycle33 21/21 保持（漂移 0.074rad 不变）；九组 cycle + 无线真机 e2e 全绿。

### 方向 V：远期目标日定时（v1.18.0，2026-09-25 凌晨）
- 动机：abs 定时原本只能「今天(或明天)的 HH:MM:SS」，无法表达「周五发布会 20:00」这类远期目标——绝对节点哲学最该服务的场景反而做不了。
- 实现：定时栏新增可选日期 input（留空=原语义逐字节不变，周期校准语义不动）；选了日期即定时到那天的那一时刻（过去时刻拒绝并提示）；核心状态机零改动（startSingle 本就收绝对 epoch，远期布防由 ActionTimeline/Coordinator 的既有「到点前才排程」规则天然覆盖）。
- 验证：cycle33 扩至 21 断言（一周后 12:00 布防，target 与期望差 <1500ms）；45 契约 1198 断言 + 九组 cycle + 无线真机 e2e 全绿。

### 方向 U：击拍黄金时段（v1.17.0，2026-09-25 凌晨）
- 动机：每日击拍统计只回答「练了多少」，这版回答「什么时候状态最好」——24 小时分布聚合出你的黄金时段。
- 实现：beat-stats 的 DayStat 扩 hours[24] 桶（record 增 hour 参数，非法桶忽略），summary 聚合近 30 天 bestHour（契约 +8 项含旧版无 hours 字段的向后兼容）；beats.js tooltip 追加「黄金时段 HH:00」。
- 坑位记录：BeatStatsSummary 接口没跟上 accuracy 字段扩展，strict 模式构建失败——接口与实现同步改。
- 验证：45 契约 1198 断言；九组 cycle（cycle33 20/20）+ 无线真机 e2e 全绿。

### 方向 T：闲置自流动（v1.16.0，2026-09-25 凌晨）
- 动机：常驻装置在你不在场时应该「自己流动」——界面退场只留时间本身，相机极缓自转让星球仍在轨道上。
- 实现：ui.js 顶层 idle 状态机（600s 无 pointer/key/wheel 输入 → body.idle；布防等触发期间拒绝进入，armed 即拉回；任意输入 2.2s 淡回）；CSS 退场规则（panel 淡出、钟面保留数字去背景、秒环仍在）；scene3d 闲置时相机 targetYaw 以 ~0.06 rad/s 缓漂；`TC.UI.forceIdle/idleState` 强制钩子（600s 边界等不起）。
- 坑位记录：idle 状态机首版误放 bindControls() 作用域内，TC.UI 出口引用会 ReferenceError——提升到 IIFE 顶层（函数声明提升可跨区注册监听）。
- 验证：cycle33 扩至 20 断言（进入类翻转+1.2s 漂移 0.075rad+退出恢复；布防拒绝）；45 契约 1193 断言 + 九组 cycle + 无线真机 e2e 全绿。

### 方向 S：太阳赤纬（v1.15.0，2026-09-25 凌晨）
- 动机：DESIGN 里「高度角 2.6 艺术定值」升级为真值——太阳高度本就该随季节在 ±23.44° 摆动，年尺度流进光照本身（与年环同源的时间叙事）。
- 实现：`src/domain/scene/solar-declination.ts` 近似公式（ε·sin(360°(N−80)/365)，二分二至锚点校准，契约 373 项含全年逐日区间钳制）；updateSun 的 key 光高度 = 2.6 + decl/23.44×0.5（视觉振幅 ±0.5 控制回归面）；`?dayofyear=N` 覆盖 + debugSun.declDeg 探针。
- 断言面推演：方位角 az、昼夜因子 day、晨昏 warm、月相/月光均不含高度项——cycle31/33 既有断言零外溢；新增夏至高度 > 冬至高度断言。
- 验证：45 契约 1193 断言；cycle33 扩至 18 断言（夏至 +23.44/冬至 −23.43 且 pos.y 夏>冬）；九组 cycle + 无线真机 e2e 全绿。

### 方向 R：到点历史统计（v1.14.0，2026-09-25 凌晨）
- 动机：每日击拍统计的倒计时对偶（§7 池内旧条目）——到点现在响完就忘，练「对齐做事」的人看不到自己的累计。
- 实现：`src/domain/countdown/fired-stats.ts` 照 beat-stats 模板（分日计数/跨日累计 total 不受 30 天滚动影响/坏数据隔离/深拷贝快照/100 里程碑翻旗仅一次，契约 12 项）；ui.js 订阅 cd:zero 计数 + localStorage 防抖持久化 + 抽屉行渲染，里程碑触发复用 `TC.Scene.meteor()`（现成瞬时流星语汇，零新增辉光类别）。
- 验证：44 契约 820 断言；cycle33 扩至 17 断言（真实 4s 倒计时到点 → 抽屉「今日到点 1 · 累计 1」）；九组 cycle + 无线真机 e2e 全绿。
- 坑位记录：①深夜屏幕关闭会让 CDP Page.captureScreenshot 挂死（GPU 合成器休眠）——门禁前先移动鼠标唤醒显示器；②用户临时插上的新 USB 手机若未开「USB 调试（安全设置）」screencap 会挂死，`adb reconnect` 可摘除待重新授权。
- 浸泡证据（03:00 空闲态，v1.19.0 代码）：跨真实整点脉冲恰好 +1 且仅一次（闲置降帧 10fps 下区间检测零漏触发）、内存恒定 10MB、零页面错误。至此真实边界矩阵闭环：空闲触发+1 / 布防让位 / 驻留窗让位，三路径均有真实验证。

### 方向 P：音效主题包（v1.12.0，2026-09-25 凌晨）
- 动机：DESIGN §7 backlog 旧条目「音效主题包」——全合成音效的可玩性延伸：同一装置四种声音性格。
- 实现：`src/domain/audio/sound-pack.ts` 纯定义（硅晶=恒等变换逐参数复刻 v1.11 行为；钟琴=正弦长衰减；木质=三角波短促；芯片=方波 8-bit；契约 39 项含「任意两包必有可区分声部」）；audio.js 的 tick/beep/judge/miss/fire 消费 timbre 四参数（wave/bright/decay/peak）；**fire 的 bright 不作用于音阶**（音程关系归昼夜主题管）；设置抽屉下拉 + tc.sndpack 持久化，未知 key 回退默认包。
- 验证：42 契约 791 断言；cycle33 扩至 15 断言（切换生效/localStorage 持久化/切回恒等）；九组 cycle + 无线真机 e2e 全绿。

### 方向 O：月相夜光 + 文档收口（v1.11.0，2026-09-19 夜）
- 动机：夜光原本只随太阳昼夜开关——月亮已经是真实月相了，夜光强度理应跟着盈亏走：满月夜明亮、新月夜沉暗，天体叙事闭环；同时 README 主图还停在 v1.5 时代的旧场景。
- 实现：moon-phase 模块新增 `illum`（(1−cos 2π·phase)/2，契约 15 项）；updateSun 的 moonLight 强度 = (1−day)·0.55·(0.35+0.65·illum)，0.35 地板保底夜面不全黑；README 主图用 ?sunhour=18&moonage=12 构图重拍（黄昏暖光 + 盈凸月悬夜空），功能清单补齐今晚六项。
- 验证：cycle33 扩至 13 断言（满月夜光 > 2× 新月夜光）；cycle31「午夜月光」断言从固定 0.4 阈值改为物理区间 [0.17, 0.50]（新月 0.173 ~ 满月 0.495，日期无关）；全量 41 契约 752 断言 + 九组 cycle + 真机 e2e 全绿。
- 坑位记录：①README 主图截图 URL 被 sed 拼出双 sunhour（页面取首个=午夜），judge 逐像素测出月亮左缘裁切与暖光缺失，实为截图参数错误而非渲染缺陷——探针证据（debugMoon.screen 投影 + debugSun.keyColor）定位后以干净参数重拍；②debugMoon 新增 screen 投影坐标与 keyColor 字段，此后构图不再靠纸面推算；③主图构图回归已获批的「午夜满月悬于星球前方」，illum 相位非线性（phase 0.474 ≈ 全亮 0.993）勿凭 phase 直觉描述明暗。

### 方向 N：整点深呼吸（v1.10.0，2026-09-19 夜）
- 动机：「到点必触发」的绝对节点哲学下沉到小时尺度——每个 HH:00:00 星球做一次极缓深脉冲，桌面装置的心跳与真实时间同步；错过多个整点（睡眠/挂起唤醒）合并为一次，不补拍。
- 实现：`src/domain/clock/hour-crossing.ts` 纯跨界检测（契约 11 项，含「桶号当 epoch」用法陷阱锁定）；scene3d 每帧检测，触发 spawnShock(0.35) + 轻粒子踢（复用到点冲击波的低强度档）；布防期间让位给倒计时阶段表现，`TC.Scene.hourPulse()` 强制钩子 + debugSun.hourPulses 计数探针。
- 坑位记录：**prev 必须传真实 epoch**——首版把小时桶号（497154）当 prevEpochMs 传入，函数视为 1970 年（桶 0）导致每帧误触发（3s 内 607 次）；实机探针抓出后修正，并把陷阱写成契约断言。另：内联 node -e 的引号在本环境不可靠，探针一律落 .codex/*.cjs 文件。
- 验证：静置 4s 计数静止（0→0）、强制 +1、布防拒绝/停止恢复（cycle33 扩至 12 断言）；全量 41 契约 749 断言 + 九组 cycle + 真机 e2e 全绿。
- 浸泡证据：03:00 真实整点边界 8 分钟浸泡——30s 无限倒计时 14 次到点零缺漏、整点脉冲恰好 +1 且仅一次、内存恒定 10MB、零页面错误。
- 浸泡②与修复（v1.11.1）：布防跨 04:00 真实边界时脉冲仍触发——04:00 恰落在 30s 周期的归零驻留窗（占空 6%），让位条件 `armed && !fired` 在驻留期放行。修正为**整个布防生命周期让位**（驻留期 armed 仍为 true，DESIGN §2.2），错过的整点按「错过不补拍」消费；cycle33 扩至 14 断言（新增驻留期让位断言）。教训：真实边界浸泡能抓到合成测试覆盖不到的相位组合。
- 浸泡③（05:00，修复后）：布防跨真实边界脉冲保持 0（14 次到点照常、内存平、零错误）——v1.11.1 修复在真实边界得到验证。至此三条真实边界路径闭环：03:00 触发（旧代码驻留窗泄漏样本）、04:00 布防让位缺陷抓出、05:00 布防让位成立；空闲触发路径由 cycle33 强制钩子与契约覆盖。

### 四形态逐像素走查（收官质量关，05:30）
- 方法：四形态夜景截图（normal/compact/clock/fullscreen）交 judge 逐像素走查（PNG 解码+连通域/文字行测量）。
- 结论：**零产品缺陷**。三项初判 FAIL 全为扫描脚本或设计内行为：①全屏图实为 280×96——脚本误发不存在的 `toggleFs` IPC（正确命令 `toggle-fullscreen`）；②钟面底部三段文字是全新 profile 首次进钟面的 3.5s 操作提示（tc.hint.clock 设计内）；③「小窗」实测 660×460 是 PRESETS 旧版别名 compact 的合法尺寸（现小窗为 small 480×320）。
- 记录：正常形态月亮 (1117,494) 距倒计时行 85px 无遮挡、一日进度环与全部文字行交叠为 0（极淡设计致无法独立测量，标注 Unverified-轻）、compact 绿色小字为校时徽章绿点。

### 方向 M：每日击拍统计（v1.9.0，2026-09-19 夜）
- 动机：DESIGN §7 backlog 挂了两周的老条目——击拍数据现在随缘消失，练拍的人看不到自己的累计。
- 实现：`src/domain/beats/beat-stats.ts` 纯状态机（分档计数/最高连击/加权准确率，30 天滚动保留，坏数据隔离，深拷贝快照，契约 13 项）；`beats.js` 只做 localStorage 持久化（防抖 800ms）与抽屉一行文本渲染；被动展示零按钮。
- 坑位记录：①summary 浅拷贝泄漏内部状态（调用方可改写统计）——契约的副本断言抓出，改深拷贝；②32 键裁 30 应裁掉前两个，断言先写错后修正；③summary 重构后 TS 严格模式抓出 spread-of-possibly-undefined。
- 验证：cycle33 扩至 10 断言（自由节拍真实击拍入库 +1 + 抽屉文本「今日击拍 N · 最高连击 M · 准确率 P%」）；全量 40 契约 738 断言 + 九组 cycle + 真机 e2e 全绿。

### 方向 L：音景昼夜四态（v1.8.0，2026-09-19 夜）
- 动机：到点音景是 signature moment，但永远同一组 C 大琶音——让释放音随一天中的时刻换色，夜晚柔和、白昼明亮，装置才「活在一整天里」。
- 实现：`src/domain/audio/daypart-theme.ts` 纯计算（边界 5/11/17/23；晨 C 大五声 / 昼 G 大五声 / 暮 E 小五声 / 夜 D 小五声，全部五声族防不协和，契约 27 项）；`audio.js` 的 S.fire 只换琶音频率表，sub 冲击与钟簇余韵原样保留，调用方传入**节点 epoch**（调度路径 cd.target / cd:zero payload target）而非调用时刻——预排发生在节点前 ≤63s，用错时刻会跨边界排错音。
- 验证：cycle33 扩至 9 断言（08晨/12昼/20暮/02夜四态映射 + 四根音互异且 8 音表）；全量 39 契约 725 断言 + 九组 cycle + 真机 e2e 全绿；听感留待用户人耳终验（结构已锁定）。

### 方向 K：一日进度环（v1.7.1，2026-09-19 夜）
- 动机：钟面秒环是「一分钟」的形状，日进度环补上「一天」的形状——清晨细线、正午半环、深夜满环，日期感一眼可见。
- 实现：`src/domain/scene/day-progress-model.ts`（hourFloat→frac，契约 12 项）；scene3d 4.75-4.85 单位外圈 MeshBasicMaterial 扁平细环（非加色、透明度 0.10，严守辉光配额——是「进度可视化」不是辉光），顶部起顺时针填充与能量环/秒环同语，几何约 4s 才重建一次。
- 验证：cycle33 扩至 7 断言（21 时→0.875、06 时→0.25、实时 0.0625=凌晨 1:30 自洽）；draw calls 43→44 证实环在渲染；全量 38 契约 698 断言 + 九组 cycle + 真机 e2e 全绿。

### 方向 J：真实月相（v1.7.0，2026-09-19 夜）
- 动机：昨夜已有真实太阳与星野，补上月亮凑齐「日/月/星」三件套；月相是唯一能从桌面读出「今天几号级别」时间尺度的天体。
- 实现：`src/domain/scene/moon-phase.ts` 纯计算（朔望周期 29.530588853 天锚定 2000-01-06 参考新月，2024-04-08 日食/2024-04-23 满月双真实锚点校准）；scene3d 挂受光小球（0.14 半径，4.4 单位倾斜轨道），明暗面由同一把太阳 key 光自然照出；新月始终位于日地连线上。
- 验证：契约 12 项（含双天文锚点）；cycle33 五断言——实时相位与同公式一致、新月方位点积=1、上弦=0、满月=−1（方位关系即物理不变量）；CDP 截图像素级走查满月盘面。
- 坑位记录：①恰半点相位受浮点影响盈亏抖动，断言用半点+偏移验证；②太阳 key 光有固定高度角，「明暗面朝向」断言必须用方位角点积（litDot 会因高度角差永远到不了 ±1）；③月亮与太阳必须同参数化（sin,cos）否则方位差 90°；④截图走查时误把能量环进度光尖认成月亮（2.24 单位/278px/青色全吻合）——像素验收需先排除已知 sprite。


> 收官备注（06:00 前）：方向 0-I 全部收口，共 8 个版本（v1.3.0～v1.5.0）。

### 方向 I：渲染降耗（v1.5.0）——含对 backlog「粒子 InstancedBufferGeometry」的评估结论
- 评估：粒子系统 900 颗 × 30fps 的 CPU 更新实测开销 ~0.05ms，缓冲上传 ~11KB/帧——重写为 InstancedBufferGeometry + 着色器积分的收益接近零，而 kickParticles/respawn 等交互逻辑迁到 GPU 会带来视觉回归风险。**结论：放弃字面方案，做真正的大头**。
- 真正的功耗黑洞：仅时间形态下 #scene 是 display:none，但 WebGL 仍对隐藏 canvas 跑完整管线（实测每帧 42 draw calls / 17136 三角形）——悬浮钟变成隐形电暖器。
- 实现：render() 检测 body.clockmode 直接整帧早退（skipStreak 连续跳帧计数暴露于 debugSun 探针）；退出形态即恢复。cycle31 新增第 8 断言守护。
> 未来候选（按 ROI 排序，未排期）：粒子 InstancedBufferGeometry 降耗、每日击拍统计面板、局域网伴侣页、音效主题包——均见 DESIGN.md §7 backlog。

### 方向 0：钟面动效四件套（v1.3.0）
- 动机：用户反馈悬浮钟面「生硬」；已在循环 #45 完成并验证（回归 70/70 + 探针实证）。
- 内容：数字 tick（变化对 240ms 上浮淡入）、tweenBounds 窗口缓动（形态/滚轮/还原统一，170ms easeOutCubic）、clockIn/panelIn 形态 morph、呼吸边（辉光配额钟面唯一例外）；附带 win:state 推送让右键进出钟面零延迟。
- 结果：全部完成，E2E 全绿，本版本随 v1.3.0 发布。

### 方向 A：行星真实太阳（v1.3.1）
- 动机：TIMECORE 是时间装置，3D 星球却永远被固定方向照亮——让星球昼夜随**真实时间**转，是「眼前一亮」且零操作成本的核心创意。
- 预期：根据本地时间计算太阳直射经度（近似：正午 12:00 照亮朝屏幕一侧），光照方向向量绕星球缓慢旋转；夜间半球压暗 + 微弱冷色，晨昏线柔和过渡；能量环/粒子不受影响。
- 风险：scene3d.js shader 改动需截图验证；性能不回退。

### 方向 B：到点音景（v1.3.2）
- 动机：到点目前是单音白闪+提示音，缺少「仪式感」；释放瞬间应该是这个装置的 signature moment。
- 预期：分层释放音（sub 冲击 + 五声音阶琶音 + 短尾混响感），复用现有 track 音量体系；不新增设置项；倒计时 10s 阶段音与自由节拍音色不动（已验证可闻性）。
- 风险：音频调度走绝对节点预排，改动集中在音色合成层，不碰调度逻辑。

### 方向 C：钟面秒环与零点脉动（v1.3.3）
- 动机：纯数字钟面缺一个「时间的形状」；一圈极细秒弧让 60 秒的循环可见，归零时整窗一次脉动把「到点必触发」变成看得见的仪式。
- 预期：SVG/conic-gradient 秒进度弧（GPU 合成，颜色中性青），归零瞬间钟面整体 1 次柔和 scale/glow 脉动；不改信息结构，不加按钮。
- 风险：钟面只有时分秒.毫秒的信息红线——弧是「秒的可视化」不是新信息，设计上要薄（≤3px）。

### 方向 D：开场仪式（v1.3.4）
- 动机：冷启动瞬间直接整屏闪现，缺少「装置通电」的感觉。
- 预期：窗口启动 300ms 淡入 + 核心能量从 0 充能一次；欢迎卡入场动画。零交互变化。
- 风险：启动耗时不能增加（动画只在渲染层，不阻塞 boot）。

### 方向 E：收口打磨（v1.4.0）
- 动机：一夜迭代的收束——巡检细节、更新 README 与截图、写版本总结。
- 预期：全量回归 + 真机走查清单 + 文档对齐。

## 发版纪律（每版本执行）

1. version bump（package.json）→ commit → tag → push
2. CI 出包 → GitHub API PATCH draft:false（UTF-8 JSON，写清本版内容）
3. 验证 releases/latest 版本号正确
4. 回写本文件状态列 + PROGRESS.md + memory
